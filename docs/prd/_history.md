# PRD 작업 히스토리

> docs/prd/domains/*.md 와 관련된 모든 작업 (IA 확정, wireframe 생성, design-sync, 코드 반영, 폐기) 을 누적 추적.
> 새 항목은 **표 위쪽에 추가** (최신이 위). 시간순 역정렬.

---

## 표기 규칙

### 컬럼

| 컬럼 | 의미 |
|---|---|
| **Date** | 작업 일자 (YYYY-MM-DD) |
| **Domain** | 대상 도메인 (예: coupons, mobile). cross-domain 작업은 `_cross` 또는 다중 도메인 콤마 구분 |
| **Type** | 작업 종류 (아래 표기) |
| **Action** | 한 줄 요약 |
| **Files Changed** | 변경 / 추가 / 삭제 파일 (path 명시) |
| **Impact** | 영향 범위 (라우트, 컴포넌트, BE endpoint 등 — Part A cite 가능하면 cite) |
| **Commit** | 관련 git commit 해시 (있다면) |
| **By** | 수행 주체 (`agent: prd-ia-interactive` / `manual` / `agent: prd-design-sync` 등) |
| **Notes** | 추가 메모 (Owner 결정 사유, follow-up 항목 등) |

### Type 분류

| Type | 의미 |
|---|---|
| `IA-CONFIRM` | prd-ia-interactive 가 Part B 확정 (`docs/prd/domains/{domain}.md` Edit) |
| `WIREFRAME` | prd-wireframe-generator 가 화면기획서 생성/갱신 (`docs/prd/wireframes/{domain}.md`) |
| `DESIGN-SYNC` | prd-design-sync 가 figma↔코드 갭 분석 산출 (`docs/prd/design-sync/{domain}.md`) |
| `CODE` | 도메인 관련 실제 코드 변경 (web/, src/main/, sql/) |
| `DEPRECATE` | 폐기 작업 (코드 삭제 + PRD 파일 삭제 등). 사유 + 영향 명시 필수 |
| `OWNER-DECISION` | ★ Owner 결정 5건 중 하나 확정 (또는 보류 → 결정 전환) |
| `RUNTIME-VERIFY` | runtime-analyzer 결과로 Part A 사실 보강 (예: row 수 검증 결과 반영) |
| `REVERT` | 이전 작업 되돌림 |
| `MIGRATE` | 폴더 구조 / 파일 이동 (예: home 을 표준 패턴으로 정렬) |
| `FIGMA-UPDATE-SPEC` | design-sync 결과 → figma 작업자용 actionable 갱신 명세서 별도 분리 (`docs/prd/figma-updates/{domain}.md`). 코드 = source-of-truth 일 때 figma 측 수정 항목 정리 |

### Files Changed 표기 컨벤션

- `+ path/to/file.md` — 신규 파일
- `~ path/to/file.md` — 수정 파일
- `- path/to/file.md` — 삭제 파일
- 동일 도메인 다수 변경 시 `path/to/dir/*` 약식 허용 (단 무엇이 바뀌었는지 Action / Notes 에 풀어 쓸 것)

### Impact 표기 컨벤션

- 라우트: `route: /coupons`
- 컴포넌트: `component: CouponScreen.jsx`
- BE endpoint: `endpoint: GET /api/coupons`
- DB 테이블: `table: site_coupons`
- 다중 도메인 영향: `cross-domain: home, community`
- 영향 없음: `none`

---

## 갱신 주체별 룰

### Agent 자동 갱신
- `prd-ia-interactive`: Part B Edit 후 종료 보고 직전에 본 파일 상단 표에 `IA-CONFIRM` row append
- `prd-wireframe-generator`: 산출물 Write 후 `WIREFRAME` row append
- `prd-design-sync`: 산출물 Write 후 `DESIGN-SYNC` row append
- 각 agent 가 본 파일에 직접 append. 표 헤더는 절대 변경 X — row 만 위쪽에 삽입

### 수동 갱신 (사용자 또는 어시스턴트)
- 코드 변경 / 폐기 / Owner 결정 / runtime 검증 / migration 은 작업 직후 사용자 또는 어시스턴트가 본 파일에 추가
- commit 해시는 commit 후 갱신 권장 (빈 칸 → 후속 채움 OK)

---

## 작업 히스토리 (최신이 위)

| Date | Domain | Type | Action | Files Changed | Impact | Commit | By | Notes |
|---|---|---|---|---|---|---|---|---|
| 2026-05-09 | kbo | DEPRECATE | BE kbo 도메인 폐기 — `src/main/java/com/dawne/com2usbaseball/domain/kbo/**` 통째 삭제 (9 Java 파일: Controller 1 / Service 2 / Repository 1 / Mapper 1 / Entity 1 / DTO 3) + `src/main/resources/mapper/kbo/KboGameMapper.xml` 삭제 + `mapper/kbo/` 디렉터리 자체 제거. SecurityConfig `/api/kbo/**` 전용 가드 없음 (`/api/**` permitAll 일괄 처리). CacheConfig 는 작업 시점 이미 전체 주석 상태 (kboMatches 캐시 등록 라인 포함) — 추가 정리 불필요. 다른 BE 도메인 의 kbo 클래스 import 0건 (grep 검증). docs/prd/domains/kbo.md 폐기 이력 섹션 신설 (FE + BE commit hash 명시 + 미처리 보류 항목 명시 + 향후 재도입 가이드) | `- src/main/java/com/dawne/com2usbaseball/domain/kbo/controller/KboGameController.java`, `- src/main/java/com/dawne/com2usbaseball/domain/kbo/service/KboGameService.java`, `- src/main/java/com/dawne/com2usbaseball/domain/kbo/service/KboGameServiceImpl.java`, `- src/main/java/com/dawne/com2usbaseball/domain/kbo/repository/KboGameRepository.java`, `- src/main/java/com/dawne/com2usbaseball/domain/kbo/repository/mapper/KboGameMapper.java`, `- src/main/java/com/dawne/com2usbaseball/domain/kbo/entity/KboGameEntity.java`, `- src/main/java/com/dawne/com2usbaseball/domain/kbo/dto/response/KboMatchResponse.java`, `- src/main/java/com/dawne/com2usbaseball/domain/kbo/dto/response/KboTeamInfo.java`, `- src/main/java/com/dawne/com2usbaseball/domain/kbo/dto/response/TodayMatchesResponse.java`, `- src/main/resources/mapper/kbo/KboGameMapper.xml`, `~ docs/prd/domains/kbo.md` (§ 폐기 이력 신설) | endpoint: GET `/api/kbo/matches/today` 폐기, endpoint: GET `/api/kbo/matches/{matchId}` 폐기, package: `domain/kbo/**` (9 파일), folder: `mapper/kbo/**` (1 xml + 디렉터리 제거), table: `kbo_*` 6개 미터치 (kbocrol 운영) | (대기) | manual | 사용자 정책 (2026-05-09): "BE 에서도 해당 web 과 통신하는 API 구현된 게 있으면 전부 삭제". FE commit (선행) 의 후속 BE 작업. ⚠ DB 테이블 (`kbo_seasons`/`kbo_teams`/`kbo_games`/`kbo_players`/`kbo_batter_logs`/`kbo_team_code_mappings`) 미삭제 — `kbocrol` Python crawler 가 DB 공유 사용 (BE 와 직접 통신 X). ⚠ `kbocrol/**` 폴더 미터치 (별도 시스템). ⚠ `sql/CREATE_TABLE_KBO.sql`·`sql/INSERT_KBO_DATA_TABLE.sql` 등 schema 파일 미삭제 (운영 데이터 보존). 글로벌 ★ Owner 결정 #4 (legacy PC 도메인 운명) 일부 해소 — kbo 한정 폐기. dictionary/simulate 는 별도 라운드 |
| 2026-05-09 | kbo | DEPRECATE | FE kbo 도메인 폐기 — `web/src/domains/kbo/**` 통째 삭제 (17 파일: config 1 / pages 1 / components 8 jsx+scss / hooks 1 / utils 1 / store 4 jsx+slices) + 연관 사용처 4개 파일 주석 처리 (추후 복원 가능성 보존). 주석 메모 통일: `// LEGACY kbo 도메인 잠정 보류 — 2026-05-09 (frontend kbo 화면 폐기. BE API 미구현 / DB 는 kbocrol 운영)` | `- web/src/domains/kbo/config/kboMatch.config.js`, `- web/src/domains/kbo/feature/public/pages/KBOLeaguePage.jsx`, `- web/src/domains/kbo/feature/public/components/{card,hero,matchSection,winRate}/*` (8 파일), `- web/src/domains/kbo/feature/public/hooks/useTodayMatches.js`, `- web/src/domains/kbo/feature/public/utils/matchUtils.js`, `- web/src/domains/kbo/store/slices.js`, `- web/src/domains/kbo/store/public/{api,endpoints,thunks}.js`, `~ web/src/app/router/routes/PublicRoutes.jsx` (KBOLeaguePage lazy import 주석), `~ web/src/app/store/store.js` (kboReducer import + reducer 등록 주석), `~ web/src/domains/home/config/QUICK_MENUS.js` (KBO 승부예측 메뉴 항목 주석), `~ web/src/app/wrapper/mobile/config/MENU_GROUPS.js` (KBO 승부예측 메뉴 항목 주석) | route: `/kbo` 진입 불가 (KBOLeaguePage lazy import 주석 + 라우트 이미 주석 상태 유지), folder: `web/src/domains/kbo/**` 17 파일 통째 삭제, store: Redux `kbo` slice 등록 제거 (번들 사이즈 감소), component: HomeScreen QUICK_MENUS · MobileLayout MENU_GROUPS 의 KBO 승부예측 항목 미노출 | `2054c37` | manual | 사용자 정책 (2026-05-09): "src/domains/kbo 폴더 삭제 + 연관 페이지에서는 주석 처리 (삭제 X) + 'fe, be' 단위로 2번 commit". 본 commit 은 FE 단위. BE 폐기는 후속 commit (별도 row). ⚠ DB 테이블 (kbo_*) 미삭제 — kbocrol Python crawler 가 DB 공유 사용 중. ⚠ kbocrol/ 폴더 미터치 (별도 시스템). 부수 효과: store.js 의 `kboReducer` 미등록으로 Redux state shape 변경 (`fe/dead-suspects.md` 정리 권고 2 자연 해소) |
| 2026-05-09 | admin | DEPRECATE | `web/src/domains/admin/` 폴더 통째 폐기 (T3 Step 3b 완료) + 4 consumer 흡수 (AdminRoutes 3 lazy import + 3 active route 주석 / AdminPageLayout 의 AdminNavigation inline 흡수 / coupons CouponTableBody 의 VisibleToggle import 갱신 / VisibleToggle 글로벌 공용 `web/src/global/ui/visibleToggle/` 이전). 직전 라운드 (commit 79c8127) Step 3a 의 후속 처리 — admin.md TODO P1 → P0 즉시 처리. 사용자 정책 (2026-05-09): "src/domains/admin 잔존 → 즉시 삭제. admin.md 업데이트 재적용". 신규 admin 페이지는 `src/domains/{domain}/feature/admin/` 패턴 (coupons 이미 적용) — 별도 admin 폴더 존재 안 함 | `- web/src/domains/admin/pages/dashboard/AdminDashBoardPage.jsx` (+scss), `- web/src/domains/admin/pages/user/AdminUserManagePage.jsx` (+scss +useAdminUserForm.js +useUserFilter.js), `- web/src/domains/admin/pages/user/AdminUserDetailPage.jsx` (+scss), `- web/src/domains/admin/config/AdminNavigation.js`, `- web/src/domains/admin/feature/components/toggle/VisibleToggle.jsx` (+scss) (= 11 파일 통째 삭제), `+ web/src/global/ui/visibleToggle/VisibleToggle.jsx` (+module.scss +index.js), `~ web/src/app/router/routes/AdminRoutes.jsx` (3 lazy import + 3 active route 주석), `~ web/src/global/layout/adminPageLayout/AdminPageLayout.jsx` (AdminNavigation inline 흡수 — `domains/admin/config` import 제거), `~ web/src/domains/coupons/feature/admin/components/table/CouponTableBody.jsx` (VisibleToggle import path 갱신), `~ docs/prd/domains/admin.md` (TODO P1 → P0 ✅ 갱신 + B.4/B.7/B.8 갱신), `~ docs/prd/_history.md` | route: `/admin` index + `/admin/users` + `/admin/users/:userId` 3 라우트 주석 처리 (활성 라우트 0건 변동 — content 하위 + community 만 활성), folder: `web/src/domains/admin/**` 통째 제거 (이전 라운드 store 6 파일 + 본 라운드 11 파일 = 17 파일 전체), folder 신규: `web/src/global/ui/visibleToggle/**` (3 파일), component: AdminPageLayout (AdminNavigation inline 화), component: CouponTableBody (import path 갱신), cross-domain: AdminRoutes / coupons admin / global layout / global ui (4 consumer 동시 수정 cross-domain commit) | (대기) | manual | 직전 라운드 (commit 79c8127) Step 3a 의 후속. 사용자 정책 (2026-05-09): "이번 배포 시점은 기존 legacy 삭제 + 신규 모바일화 된 것 기준으로 진행" + "각 도메인 하위 admin 패턴 (coupons 적용)". 4 consumer 흡수 결정: AdminRoutes 3 라우트는 신규 admin home / user 관리 별도 기획 미진행 → 라우트 자체 주석 + 파일 삭제 채택 (legacy sample 폐기). AdminNavigation 은 외부 사용처 1건만 이라 별도 config 분리 불요 → AdminPageLayout 내부 inline. VisibleToggle 은 events/notices/quiz admin 도 사용 예정 (notices/events Part B 의 visible toggle capability) → 글로벌 공용 채택 (coupons 자체 흡수 vs 글로벌 중 글로벌). `@/domains/admin` import 활성 사용 0건 grep 검증 완료 (주석 잔존만) |
| 2026-05-09 | historyMode | MIGRATE | `web/src/data/historyMode/` 의 mock 데이터 2개 → `web/src/domains/historyMode/config/` 로 이동 + 파일·변수 이름 의미 명확하게 재정의. 사용처 `useHistoryMode.js` import path + identifier 갱신. 도메인 표준 mock 패턴 정렬 (home/config/MOCK_QUIZ.js·MOCK_TEAM_POSTS.js·MOCK_POSTS.js 와 동일 — `domain/config/MOCK_<NAME>.js` 파일 + `MOCK_<NAME>` SCREAMING_SNAKE_CASE export) | `+ web/src/domains/historyMode/config/MOCK_HISTORY_LEGENDS.js` (구 LegendMeta.js — 도메인 사용처 cite 주석 추가), `+ web/src/domains/historyMode/config/MOCK_HISTORY_STAGES.js` (구 LegendStuff.js — 도메인 사용처 cite 주석 추가), `- web/src/data/historyMode/LegendMeta.js`, `- web/src/data/historyMode/LegendStuff.js`, `~ web/src/domains/historyMode/mobile/hooks/useHistoryMode.js` (import path + identifier 일괄 교체: `legendMeta`→`MOCK_HISTORY_LEGENDS`, `legendStuff`→`MOCK_HISTORY_STAGES`) | folder: `web/src/data/historyMode/` 폴더 자체 제거 (data/ 하위 community·skill 만 잔존), component: `useHistoryMode.js` (mock 소스 import 경로·identifier 변경 — 동작·로직 변동 없음), route: `/mode/history` (구현 동작 무영향), cross-domain: 없음 (sole consumer = `useHistoryMode.js`, grep 검증) | (대기) | manual | 사용자 정책 (2026-05-09): "src/app/data/historyMode/ 의 data 2개는 src/domains/historyMode/ 에서 사용하는 data → 도메인 하위로 이동, 다른 개발자가 폴더 봤을 때 무엇/어떤 사용처인지 즉시 명확하게". 실제 위치는 `src/data/historyMode/` (사용자가 언급한 `app/data` 는 동일 의미로 추정 — 본 라운드는 src/data/historyMode 대상). 이름 재정의 근거: 다른 도메인 (home) 의 mock 패턴 = `MOCK_<NAME>.js` 파일 + `MOCK_<NAME>` export 일관성. 파일 컨텐츠 변동 없음 (구조·필드·값 그대로 — 주석에 사용처 cite 만 추가). docs (`docs/specs/fe/state-and-data.md`, `docs/prd/domains/historyMode.md`, `docs/prd/wireframes/historyMode.md`, `docs/map/fe-map.md`, `docs/prd/figma-updates/historyMode.md`, `docs/specs/fe/api-calls.md`, `docs/specs/fe/dead-suspects.md`) 의 stale path 표기는 본 라운드 task 외 — 후속 docs 동기화 라운드에서 일괄 갱신 |
| 2026-05-09 | admin | DEPRECATE | `web/src/admin/` 폴더 통째 폐기 (legacy PC 버전 admin 도메인 샘플) + admin.md frontend admin 구현 정책 명시. 작업 시점에 폴더 자체가 이미 부재 + 외부 import 0건 검증 (`@/admin`, `src/admin`, `../admin`, `../../admin` 모두 grep 매치 0) — 따라서 실제 코드 변경 0건 (삭제할 파일 없음 / 주석 처리할 연관 페이지 없음). PRD 문서 기록만 진행 — admin.md B.7 § legacy `web/src/admin/` 폐기 + § Frontend admin 구현 정책 신설, TODO P0 완료 항목 추가 | `~ docs/prd/domains/admin.md` (B.7 신규 2 섹션 + TODO P0 완료 항목), `~ docs/prd/_history.md` | folder: `web/src/admin/**` (이미 부재), 삭제 파일 0건 (폴더 자체가 작업 시점 부재), 주석 처리 파일 0건 (외부 import 0건 grep 검증), 모바일 활성 도메인 영향 0건 (home/coupons/events/notices/quiz/historyMode/profile/authentication/community 모두 무관) | (대기) | manual | 사용자 정책 (2026-05-09): "frontend admin 구현은 각 도메인 하위 (`src/domains/{domain}/feature/admin/`). coupons 가 이미 적용 (`web/src/domains/coupons/feature/admin/`). 별도 admin 폴더 (`web/src/admin/` 또는 `web/src/domains/admin/`) 존재 안 함". 본 task 는 `web/src/admin/` (legacy PC 샘플) 한정 — `web/src/domains/admin/` (별도 폴더, T3 Step 3a/3b 대상) 와 무관. git 추적 이력에도 본 path 기록 없음 → 이전 라운드 또는 git 추적 외부에서 이미 정리됨 추정. PRD 정책 명시로 후속 라운드 혼동 방지 |
| 2026-05-09 | _cross | DEPRECATE | `web/src/core/` 폴더 통째 폐기 + 연관 legacy 파일 정리 (admin filter 용도. 모바일 버전 admin 새 기획 정책). core 5 파일 + community PC-legacy chain 3 파일 삭제 — `usePostUserFilter.js` (직접 core importer) → `feature/components/user/CommunityPage.jsx` (usePostUserFilter consumer) → `page/user/UserCommunityPage.jsx` (CommunityPage wrapper). `PublicRoutes.jsx` 의 dead lazy import 1줄 + 주석 처리된 route 1줄 제거 | `- web/src/core/filters/index.js`, `- web/src/core/filters/CoreSearchFilter.jsx`, `- web/src/core/filters/CoreVisibleFilter.jsx`, `- web/src/core/filters/CoreStatusFilter.jsx`, `- web/src/core/filters/hooks/useFilterPipline.js`, `- web/src/domains/community/feature/hooks/user/post/usePostUserFilter.js`, `- web/src/domains/community/feature/components/user/CommunityPage.jsx`, `- web/src/domains/community/page/user/UserCommunityPage.jsx`, `~ web/src/app/router/routes/PublicRoutes.jsx` (UserCommunityPage lazy import + 주석 route 줄 제거) | none — admin 관련 legacy. 모바일 활성 도메인 (community) 의 mobile 진입은 `CommunityScreen.jsx` 사용 — 영향 0 (PC-legacy `UserCommunityPage` 라우트는 이미 주석 처리 dead). cross-domain: community PC-legacy chain (mobile 미사용) | (대기) | manual | 사용자 정책 (2026-05-09): "추후 admin 페이지들은 src/domains/{domain}/feature/admin/ 패턴 (coupons 가 이미 적용 — `web/src/domains/coupons/feature/admin/`). 따로 admin 폴더 존재하지 않을 예정". core 는 admin 페이지 filter 처리용 코드였고, 모바일 버전 기준 admin 새 기획 중이라 legacy. 직접 core importer 1건 (`usePostUserFilter`) 만 외부에 존재 — community PC-legacy chain 으로 이어짐. 활성 mobile community (`domains/community/mobile/CommunityScreen.jsx`) 는 core 비의존. 잔여 PC-legacy 흔적 (PostUserTable*, useUserPost, useUserBoard, CommunityUserBoardTabs, BoardTabs, global/layout/userLayout/*) 은 본 라운드 제외 — core 비의존 + 다른 PC-legacy 정리 라운드 대상 |
| 2026-05-09 | admin | CODE | T3 Step 3a 즉시 정리 — `web/src/domains/admin/store/**` 6 파일 통째 삭제 (api/endpoints/thunks/dto/slices/index). 모두 dead — 5 파일 전체 주석 (events 구버전), `slices.js` 만 createSlice 살아있으나 `app/store/store.js` reducer 미등록 + 외부 import 0건 grep 검증 완료. admin.md TODO 섹션 신설 (P0/P1/P2/잠재위험 4 카테고리 정리 — 사용자 진술 "코드 방대+꼬인 상태, legacy 삭제+모바일 기준 배포") | `- web/src/domains/admin/store/api.js`, `- web/src/domains/admin/store/endpoints.js`, `- web/src/domains/admin/store/thunks.js`, `- web/src/domains/admin/store/dto.js`, `- web/src/domains/admin/store/slices.js`, `- web/src/domains/admin/store/index.js`, `~ docs/prd/domains/admin.md` (TODO 섹션 추가) | folder: `web/src/domains/admin/store/**` 6 파일 (전체), import 0건 (grep 검증), Redux store 영향 0 (admin reducer 미등록), cross-domain 영향 0 | (대기) | manual | Step 3b (`pages/`, `config/`, `feature/`) 는 4 consumer (AdminRoutes 3 lazy import / AdminPageLayout AdminNavigation / coupons CouponTableBody VisibleToggle) 살아있어 본 라운드 미진행 — TODO P1 으로 분리. cross-domain commit 위험 회피 (사용자 정책: "다른 도메인 미합의 변경 금지"). dead-confirmed.md 1-C 가 명시한 3 파일 (api/endpoints/thunks) + 추가 dead 3 파일 (dto 전체 주석 / slices 미등록 / index 죽은 모듈 re-export) 통합 처리 |
| 2026-05-09 | historyMode | FIGMA-UPDATE-SPEC | historyMode figma 갱신 명세서 신설 (figma 작업자 actionable). 사용자 제공 figma 3 frame (66-688 최종본 / 18-2516 EmptyState ⚾ / 66-652 EmptyState 📋) MCP 직접 검증 + wireframe figma 미반영 사항 통합 → 5 actionable (P0×1 U1 도메인 PageHeader 제거 / P1×3 U2 PlayerRow apostrophe / U3 v0.2.0 제거 / U4 StageCard selected variant / P2×1 U5 EmptyState 컴포넌트 단일화). 코드 = source-of-truth (사용자 정책 재확인 — BE 통신 없이 FE 단순 계산 로직) | `+ docs/prd/figma-updates/historyMode.md`, `~ docs/prd/wireframes/historyMode.md` (§ 7 figma 미반영 사항 항목 추가 + § 7.1 사용자 제공 3 frame 매핑 + § 7.2 figma-updates 명세 cross-ref), `~ docs/prd/_history.md` | figma file `VCVQzOpSIpwpZw11gxG7N1` node-id=66-688 / 18-2516 / 66-652 (3 frame) + 이전 라운드 4 frame 통합 (18-1908 / 18-1949 / 66-544 / 66-688), component: HistoryModeScreen.jsx + Chip.jsx + StageCard.jsx, route: `/mode/history` | (대기) | manual | 사용자 명시 (2026-05-09): "historyMode 컨텐츠는 BE 통신 없이 FE 단순 계산 로직으로 작동" + "사용자 선택 단계에 의해 화면 전환 (state machine 형태)" + "코드 = source of truth, figma 가 코드 기준으로 갱신". BE 호출 0건 grep 검증 완료 (axios/fetch/dispatch/useSelector 0 매치). 사용자 제공 3 frame 중 18-2516/66-652 는 신규 상태 아니라 기존 4 상태 안 EmptyState 컴포넌트의 figma 단독 정의 frame — 상태 매핑 변동 없음. design-sync 보류 유지 (mock-only — BE 도입 후 별도 라운드) |
| 2026-05-09 | historyMode | CODE | T1 dead 파일 제거 — `web/src/data/HistoryMode.js` (대문자 H, 단수) 삭제. Part B v1 P1 T1 acceptance criteria 충족 (전체 코드베이스 grep `data/HistoryMode` 매치 0 건 검증 — docs 만 잔존 자기 참조) | `- web/src/data/HistoryMode.js` (942 lines historyModeData 정적 mock — import 0건 dead) | none (import 0건이라 라우트 / 컴포넌트 영향 없음). 활성 데이터 `@/data/historyMode/{LegendMeta,LegendStuff}.js` 변동 없음 | (대기) | manual | Part A.7 dead 항목 / Part B.2 P1 T1 / `fe/dead-suspects.md A` 정합. 신규 기여자가 `data/historyMode/` (소문자 폴더 — 활성) 와 `data/HistoryMode.js` (대문자 단일 파일 — dead) 중 어느 것이 활성인지 헷갈리지 않도록 정리 |
| 2026-05-09 | historyMode | WIREFRAME | wireframe figma node 매핑 보강 — § 7 figma 미반영 사항에 PlayerRow apostrophe 누락 / StageCard selected variant 미정의 / EmptyState 컴포넌트 단일화 3 항목 추가 + § 7.1 사용자 제공 3 frame 역할 매핑 (66-688/18-2516/66-652) + § 7.2 figma-updates 명세 cross-ref | `~ docs/prd/wireframes/historyMode.md` (§ 7 / § 7.1 / § 7.2) | figma: node 18-2516 (EmptyState ⚾) / 66-652 (EmptyState 📋) 신규 매핑 추가 — 이전 4 frame (18-1908 / 18-1949 / 66-544 / 66-688) 매핑 유지, route: `/mode/history`, component: HistoryModeScreen.jsx | (대기) | manual | 사용자 제공 3 frame MCP 직접 검증으로 figma 미반영 사항 5건 → 3건 추가. 사용자 정책 재확인 (BE 통신 없이 FE 단순 계산 로직) 명시 |
| 2026-05-09 | admin | OWNER-DECISION | admin 도메인 한정 결정 — `web/src/domains/admin/` 폴더 운명: ✅ **(B) 통째 폐기 — 단계 분리** (Step 3a P0 `store/` 6 파일 즉시 삭제 / Step 3b P1 후속 라운드 — `pages/dashboard/`, `pages/user/`, `config/`, `feature/components/toggle/` 흡수 후 폴더 제거). 4 consumer 흡수 매핑 명시 (AdminRoutes 3 페이지 / AdminPageLayout 의 AdminNavigation / coupons admin 의 VisibleToggle) | `~ docs/prd/domains/admin.md` (Part B.7/B.8/B.9), `~ docs/prd/_history.md` | folder: `web/src/domains/admin/**` (17 파일 통째 — pages 3 + scss 4 + hooks 2 + config 1 + feature 2 + store 6), consumer: AdminRoutes.jsx 3 lazy import / AdminPageLayout.jsx 1 import / coupons admin CouponTableBody.jsx 1 import | (대기) | manual | Owner 진술 (2026-05-09): "해당 부분은 현재 삭제하고 만들게 된다면 각 도메인에서 새로 만들어야 하는 영역. 거의 layout 에 가깝거나 sample 로 만들어둔 페이지에 가까움. 그래서 해당 폴더 삭제 후 추후에 각 domain 에서 admin page 를 만드는 것으로 대체". 글로벌 ★ Owner 결정 5건과 무관 (도메인 한정 결정) — 추적 표 갱신 X |
| 2026-05-09 | admin | IA-CONFIRM | Part B v1 확정 (background 모드 — Part A + 횡단 도메인 특성 + Owner clarification 흡수). 횡단 PC 어드민 — 도메인별 admin 화면은 각 도메인 PRD 흡수 (coupons T2 / events T1·T2·T3 / notices T1·T2·T3·T6 / quiz T2 / authentication T2(R5)·T3(R6) / playerCard·community 별도 라운드). 본 도메인은 다른 도메인이 흡수하지 못한 횡단 인프라 task 만: P0 2건 (T1=AdminRoutes 통합 가드 검증 / T3 Step 3a=`store/` 6 파일 즉시 삭제) + P1 2건 (T2=Swagger 운영 노출 정책 / T3 Step 3b=`domains/admin/` 통째 폐기 후속 라운드). 신규 화면 / API / 테이블 0건. wireframe / design-sync n/a (PC 어드민) | `~ docs/prd/domains/admin.md`, `~ docs/prd/_history.md` | route: `/admin/**` (5 활성 + 1 주석 event), endpoint: `/api/admin/**` 가드 (T1 검증), folder: `web/src/domains/admin/**` (T3 폐기 — Step 3a/3b), file: `SecurityConfig.java:43-54` (T1/T2 가드), cross-domain: events·coupons·playerCard·notices·quiz·community (T1 통합 검증) / authentication (R5/R6 흡수 매핑) / profile·user (Step 3b user 페이지 흡수 대상) | (대기) | agent: prd-ia-interactive | background 모드 — Part A + 횡단 도메인 특성 기반 default. R4 → events.md T3 / R5 → authentication.md T2 / R6 → authentication.md T3 흡수 매핑 명시 (admin 도메인 본 라운드 중복 task 화 X). 진행 중 Owner clarification (2026-05-09: `domains/admin/` 통째 폐기) 흡수 → T3 단계 분리 (Step 3a P0 즉시 / Step 3b P1 후속 라운드). wireframe / design-sync n/a (PC 어드민). 글로벌 ★ Owner 결정 5건과 무관 |
| 2026-05-09 | historyMode | WIREFRAME | 화면기획서 신규 작성 — 단일 화면 4 상태 wireframe (상태1=초기/상태2=구단선택/상태3=선수선택/상태4=스테이지선택). figma 매칭 4/4 (18-1908/18-1949/66-544/66-688). 글로벌 TopBar 사용 명시 (도메인 PageHeader 금지). Chip+StageCard 만 분리 컴포넌트 표기. 신규 컴포넌트 없음 | `+ docs/prd/wireframes/historyMode.md` | route: `/mode/history`, figma: node 18-1908 / 18-1949 / 66-544 / 66-688 (4상태), component: HistoryModeScreen.jsx (4상태 인라인) + Chip.jsx + StageCard.jsx | (대기) | agent: prd-wireframe-generator | figma 4/4 매칭 완료. design-sync 보류 — mock-only (BE 도입 후 별도 라운드). figma 미반영: C/PageHeader 도메인 헤더 → 글로벌 TopBar 정렬 요청 필요 (design-sync 재진행 시). 신규 컴포넌트 없음 (기존 구현 완료) |
| 2026-05-09 | authentication | IA-CONFIRM | Part B v1 확정 (background 모드 — Part A 위험 R3/R5/R6 기반 default 진행). P0 2건 (T1=R3 FE state.auth shape fix — slice setter 오타 정정 `useRole`→`userRole` + 소비처 `authority` → `user`+`userRole` 일괄 교체 / T2=R5 BE `/api/dev/test-token` 보안 가드 — `@Profile("!prod")` 분기 채택) / P1 1건 (T3=R6 `@EnableMethodSecurity` 활성화 채택). R4 events 도메인 흡수로 제외. 신규 API/테이블 0건. wireframe / design-sync n/a (인프라성) | `~ docs/prd/domains/authentication.md`, `~ docs/prd/_history.md` | route: `/auth/callback` (UI 없음), endpoint: GET `/api/users/me` (T1 회귀 — shape 변경 없음), endpoint: GET `/api/dev/test-token` (T2 prod 비활성화), file: `web/src/domains/authentication/store/slices.js:15` (T1 destructure 정정), file: `web/src/domains/profile/page/UserProfile.jsx:7,24-65` (T1 authority→user 교체), file: `web/src/domains/authentication/hooks/useAuthentication.js:14,35` (T1 authority 제거), file: `src/main/java/com/dawne/com2usbaseball/domain/admin/controller/SwaggerController.java:14` (T2 @Profile), file: `src/main/java/com/dawne/com2usbaseball/config/SecurityConfig.java:25` (T3 @EnableMethodSecurity), cross-domain: profile (UserProfile authority 참조 교체), cross-domain: admin / playerCard (T3 @PreAuthorize 활성화 — coupons/events/playerCard 3 admin 컨트롤러 영향) | (대기) | agent: prd-ia-interactive | background 모드 — Part A 위험 R3/R5/R6 기반 default 진행. R3 (FE state.auth shape) P0, R5 (BE dev/test-token) P0, R6 (@PreAuthorize 활성화) P1. R4 는 events 도메인 흡수로 제외. live 소비처는 slice = SoT 채택 (소비처가 `state.auth.user` + `state.auth.userRole` 로 적응). dead 소비처 (`useHeaderAuth.js`, `Header.jsx`) 는 `_overview.md § 6.1` dead 정리 별도 라운드. R6 옵션 (a) `@EnableMethodSecurity` 활성화 채택 (URL 가드 + method 가드 이중 방어). wireframe / design-sync n/a (인프라성). 글로벌 ★ Owner 결정 5건과 무관 → 추적 표 갱신 X. _overview.md § 7 Phase 0 의 R3 차단 fix 본 라운드 흡수 |
| 2026-05-09 | historyMode | IA-CONFIRM | Part B v1 확정 (background 모드 — memory 정책 기반 default 진행). mock-only 분류 — P0 0건 / P1 1건 (T1=dead 파일 `web/src/data/HistoryMode.js` 제거) / P2 0건. 신규 기능 0건 (BE 도입 전까지 정의 보류). memory 3건 합의 반영 (4 상태 단일 화면 / 글로벌 TopBar 통일 / 컴포넌트 분해 최소화) | `~ docs/prd/domains/historyMode.md`, `~ docs/prd/_history.md` | route: `/mode/history` (모바일 단일), component: `HistoryModeScreen.jsx` (4 상태 분기 인라인) + `Chip.jsx` + `StageCard.jsx`, file: `web/src/data/HistoryMode.js` (T1 dead 삭제 대상), endpoint: 없음 (mock-only), table: 없음 (mock-only), figma: node 18-1908 / 18-1949 / 66-544 / 66-688 (4 상태) | (대기) | agent: prd-ia-interactive | background 모드 — memory 정책 (4 상태 단일 화면 / 글로벌 헤더 / 컴포넌트 분해 최소화) 기반 default 진행. mock-only 분류 — design-sync 는 BE 도입 후 별도 라운드 (A.6 위험 1 cite). A.8 ★ Owner 결정 (BE 연동 필요성) 보류 유지 — 현재 mock 운영 안정. T1 dead 파일 grep 검증 완료 (`data/HistoryMode` 매치 0건) |
| 2026-05-09 | notices | OWNER-DECISION | notices 도메인 한정 ★ 결정 해소 — `A.8` admin notice update method/path spot-check 후 fix 결정 → ✅ **FE 를 BE 표준에 맞춤** (PUT `/admin/notices/{id}` + PATCH `/admin/notices/{id}/visible`). T2/T3 가 fix task | `~ docs/prd/domains/notices.md` (Part B.8) | endpoint: PUT `/api/admin/notices/{id}` + PATCH `/api/admin/notices/{id}/visible` (BE 측 미변경, FE 측 정합), component: `domains/notices/store/admin/api.js:6,7` | (대기) | manual | BE 가 RESTful 표준 형태로 이미 노출됨 + coupons/events 와 일관성 유지 사유. 글로벌 5건 추적 표 갱신 X (도메인 한정 결정) |
| 2026-05-09 | notices | IA-CONFIRM | Part B v1 확정 (background 모드 — 사용자 default 정책으로 진행) — P0 5건 (T1=admin 폴더 표준화 / T2=admin update method/path fix (PATCH→PUT `{id}`) / T3=admin visible path fix (`/visible`→`/{id}/visible`) / T4=public `/notice/:id` detail endpoint 연결 (`GET /notices/{id}` 단건 fetch) / T6=admin 4-capability 동작 정합 검증 (list/create/update/visible toggle)). T5 결번 (횡단 dead 정리 별도 라운드 사유 명시). 신규 API/테이블 0건. coupons/events 표준 패턴 정렬 | `~ docs/prd/domains/notices.md`, `~ docs/prd/_history.md` | route: `/notices` + `/notice/:id` (public 유지), route: `/admin/content/notice` (T1 폴더 표준화 대상), endpoint: PUT `/api/admin/notices/{id}` (T2), endpoint: PATCH `/api/admin/notices/{id}/visible` (T3), endpoint: GET `/api/notices/{id}` (T4 — BE 노출 상태 미연결 해소), endpoint: admin 4건 (T6 QA 대상), table: `site_notices` (mapper 10 stmt — 변경 없음), component: `AdminNoticeManagePage.jsx` 폴더 이동 (T1), component: `domains/notices/store/admin/api.js:6,7` (T2/T3 정합), component: `useNoticeDetail` (T4 단건 fetch 전환), cross-domain: home (HomeScreen NoticeSection — T4 detail flow 변경 영향) | (대기) | agent: prd-ia-interactive | background 모드 — 사용자 default 정책으로 진행. T1/T2/T3/T4/T6 P0, T5 결번 (횡단 dead 별도 라운드). coupons/events 표준 패턴 정렬. ★ Owner 결정 (A.8) 해소 — FE 를 BE 표준에 맞춤. public figma URL 미제공 → wireframe 단계 텍스트 모드 / admin figma 미진행 (coupons/events 패턴 그대로). dead code (NoticeLayout.jsx, CafeNotice.js, FunNotice.js) 본 라운드 task 화 안 함. admin 4-capability QA (T6) events T2 패턴 동일 |
| 2026-05-09 | events | DESIGN-SYNC | 갭 25건 분석: 통일성 위반 2 / 디자인 토큰 차이 8 / 레이아웃 차이 4 / 구현 방식 차이 1 / figma 내부 inconsistency 1 / state 미반영 2 / 일치 OK 4 / spot-check 6. figma 갱신 요청 13건 (P0×2 / P1×8 / P2×3) / 코드 수정 제안 1건 (날짜 텍스트 color) | `+ docs/prd/design-sync/events.md` | component: EventScreen.jsx, EventListVertical.jsx, EventListHorizontal.jsx, EventCard.jsx, route: `/events` + `/` (HomeScreen 미니), figma: node 16-775 + 3-18 | (대기) | agent: prd-design-sync | 통일성 위반 ★ V1 도메인 PageHeader F1 P0 / V2 섹션 타이틀 13→15px F3 P1. T5 dead navigate fallback (EventCard.jsx:13) spot-check S1 별도 표기. expired 카드 배경 / 미니 카드 title 크기 / 섹션 구분 rect Owner 결정 대기. admin n/a (figma 미진행 + 컴포넌트 부재) |
| 2026-05-09 | events | WIREFRAME | 화면기획서 신규 작성 — 화면 3개 (EventScreen 풀화면 / EventListHorizontal HomeScreen 미니 / AdminEventListPage 텍스트 wireframe). figma 매칭 2/2 (16-775=풀화면, 3-18=HomeScreen 미니). admin figma 미정 → 텍스트 wireframe. T5 dead navigate fallback 제거 인터랙션 명시. R4 (T3) admin 화면 보안 명시 | `+ docs/prd/wireframes/events.md` | route: `/events`, route: `/` (EventListHorizontal), route: `/admin/content/event` (T1 라우트 주석 해제 대상), figma: node 16-775 + 3-18, component: EventScreen.jsx / EventListHorizontal.jsx / AdminEventListPage.jsx(신규 T1) | (대기) | agent: prd-wireframe-generator | figma 매칭 2/2 완료. admin figma 미연결 (admin figma 미진행 — 텍스트 wireframe 정의). design-sync: EventScreen + EventListHorizontal (live, 권장) / AdminEventListPage (보류 — figma 미진행 + T1 미구현). T5 카드 탭 = externalLink 외부 이동만 (fallback 제거 후 기준) |
| 2026-05-09 | events | IA-CONFIRM | Part B v1 확정 (정정 라운드) — P0 4건 (T1=admin 라우트 활성화 + AdminEventListPage 컴포넌트 신규 작성 / T2=admin 4 capability 동작 정합 검증 (list/create/update/visible toggle) / T3=R4 `POST /api/upload/events` admin 가드 (BE 보안) / T5=dead navigate fallback 제거 `EventCard.jsx:13`). T4 결번 (drop, 외부 URL HTTPS = admin 책임). 신규 컴포넌트 1건 (AdminEventListPage), 신규 API/테이블 0건 | `~ docs/prd/domains/events.md`, `~ docs/prd/_history.md` | route: `/events` (public 유지), route: `/admin/content/event` (라우트 주석 해제 + 컴포넌트 신규), endpoint: GET/POST/PATCH `/api/admin/events*` 4건 (T2 검증 대상), endpoint: POST `/api/upload/events` (T3 R4 fix 대상), table: `site_events` (mapper 6 stmt), component: `AdminEventListPage.jsx` 신규, component: `EventCard.jsx:13` dead navigate 제거 | (대기) | agent: prd-ia-interactive | P0 4건 (T1=admin 라우트+컴포넌트, T2=admin 4cap QA, T3=R4 보안 가드, T5=dead navigate 제거). T4 결번 (drop). agent 1차 Edit 에서 T2 의미 hijacking ("admin 4 capability QA" → "카드 단순 노출") + P2 백로그 ("배지·필터·소트") 임의 추가 발생 → 본 라운드에서 합의안대로 정정. 카드 인터랙션 = (가) 외부 deeplink (`event.externalLink`). admin Figma 미진행 (텍스트 wireframe → design-sync 보류) |
| 2026-05-09 | coupons | DESIGN-SYNC | 갭 20건 분석: 통일성 위반 2 / 디자인 토큰 차이 6 / 레이아웃·구조 차이 4 / 구현 방식 차이 3 / figma 내부 inconsistency 1 / state 미반영 2 / 코드 risk spot-check 2. figma 갱신 요청 10건 / 코드 수정 제안 2건 | `+ docs/prd/design-sync/coupons.md` | component: CouponScreen.jsx, CouponListHorizontal.jsx, CouponCard.jsx, route: `/coupons` + `/` (HomeScreen 미니), figma: node 16:624 + 10:2 | (대기) | agent: prd-design-sync | 통일성 위반 ★ V1 도메인 PageHeader (F1 P0) / V2 PrimaryActionButton "쿠폰 적용하기" (F2 P0). 코드 risk: S4 formatNow 시그니처 불일치(T4 연동) / S5 http 평문 하드코딩(T5 연동). 토큰 미정의: --color-coupon-expired-bg 코드 추가 필요(C2). admin n/a |
| 2026-05-09 | coupons | WIREFRAME | 화면기획서 신규 작성 — 화면 3개 (CouponScreen 풀화면 / CouponListHorizontal 미니 / AdminCouponListPage 텍스트 wireframe). figma 매칭 2/2 (16-624=풀화면, 10-2=미니 가로 스크롤). admin figma 미정 → 텍스트 wireframe 정의. 컴포넌트 재사용 6건 + 신규 2건 (AdminCouponListPage, CouponFormModal). T4/T5 인터랙션 명시 | `+ docs/prd/wireframes/coupons.md` | route: `/coupons`, route: `/` (CouponListHorizontal), route: `/admin/content/coupon` (T2 라우트 주석 해제 대상), figma: node 16-624 + 10-2, component: CouponScreen.jsx / CouponListHorizontal.jsx / AdminCouponListPage.jsx(신규) | (대기) | agent: prd-wireframe-generator | figma 매칭 2/2 완료. admin figma 미연결 (figma 미진행 — 신규 텍스트 wireframe 정의). design-sync: Screen 1/2 (live, 권장) / Screen 3 (보류 — figma 미진행 + T2 미구현). T4 시간대 정합 / T5 HTTPS 인터랙션 명시만 (결정은 구현 단계 위임, B.9) |
| 2026-05-09 | quiz | FIGMA-UPDATE-SPEC | quiz design-sync 결과 → figma 갱신 명세서 분리 (figma 작업자용 actionable). design-sync 갱신 요청 6건 (F1~F6) + spot-check 3건 (S1/S2/S4) 통합 정리 → 8개 actionable 항목 (P0×2 / P1×5 / P2×1). U7 = v2 scope 외 정답 제출/보상 모달 figma 제거 명시 | `+ docs/prd/figma-updates/quiz.md`, `~ docs/prd/_history.md` | figma file `VCVQzOpSIpwpZw11gxG7N1` node-id=2-34 (Quiz Section frame), component: QuizSection.jsx, route: `/` (HomeScreen QuizSection) | (대기) | manual | 사용자 명시 — 코드 = source of truth, figma 가 코드 기준으로 갱신되어야 함. spot-check 3건 ('정답 제출/보상 모달' figma 제거 포함) 모두 figma 측 작업 항목으로 정리. Type 분류 표에 `FIGMA-UPDATE-SPEC` 신설. design-sync S3 (empty state hidden=true) 는 F6 와 통합되어 U6 1건으로. 검증: figma 수정 후 prd-design-sync 재실행 시 갭 항목 감소 (10건 → 통일성 OK 3건만 잔존 기대) |
| 2026-05-09 | coupons | OWNER-DECISION | 글로벌 ★ #1 (coupon dual-write 정책) ◐ 부분 해소 — (B) 단방향 정리 채택 + 3 단계 분리 (Step 1 P0 코드 단방향 / Step 2 P1 V1→V2 마이그 / Step 3 P2 별도 라운드 V1 DROP) | `~ docs/prd/domains/coupons.md` (Part B.7) | endpoint: GET `/api/coupons` 동작 동일, table: `coupons` (V1 read fallback 제거 예정) ↔ `site_coupons` (V2 active), service: `CouponAdminServiceImpl#createCoupon` NPE 위험 (`A.6` R1) 자연 해소 | (대기) | manual | dual-write 굳히기 (A) 는 service 수정 필요 → V2-only 가 이미 안정 동작 중이라 V1 read fallback 정리가 risk 최소. 선결: runtime-analyzer V1 vs V2 row 수 비교 (Step 2 마이그 input). 글로벌 5건 추적 표 #1 row 갱신 (☐ 미결 → ◐ 부분 해소) |
| 2026-05-09 | coupons | IA-CONFIRM | Part B v1 확정 — 사용자 4 라운드 대화. P0 5건 (T1~T5: BE 코드 단방향 전환 / admin 라우트 활성화 + AdminCouponListPage 신규 / admin 4 capability 정합 검증 / public 시간대 정합 fix / 외부 URL HTTPS+env) / P1 1건 (T6 V1→V2 INSERT only 마이그) / P2 1건 (T7 V1 테이블 DROP 별도 라운드). 신규 기능 = T2 에 흡수 (BE/DB 영향 0). figma node 2건 (16-624, 10-2 — public 모바일) | `~ docs/prd/domains/coupons.md` | route: `/coupons` (public), route: `/admin/content/coupon` (P0 활성화), endpoint: GET `/api/coupons` + admin 4건 (`A.3`), component: AdminCouponListPage 신규 (T2), service: `CouponServiceImpl#selectCouponById` (T1), table: `site_coupons` (active) ↔ `coupons` (V1 read fallback 제거 + 보존), figma: node-id=16-624 + 10-2 | (대기) | agent: prd-ia-interactive | P0 5건 (T1~T5) / P1 1건 (T6) / P2 1건 (T7). admin 활성화는 P0 (모바일 리뉴얼 일환). agent 1차 Edit 에서 우선순위 강등 + T4/T5 누락 + admin deeplink 임의 추가 발생 → 본 라운드에서 합의안대로 정정. AdminCouponListPage 컴포넌트 파일 부재 확인됨 — T2 acceptance 에 신규 작성 명시. figma node-id=16-624, 10-2 (public 모바일) — wireframe-generator 가 `/coupons` 풀 vs HomeScreen 미니 매칭 해소. T4 시간대 정합 (KST vs server-side 필터) + T5 withhive.me HTTPS 지원 여부 = 구현 단계 결정 |
| 2026-05-09 | quiz | WIREFRAME | 화면기획서 v2 완전 재생성 — HomeScreen QuizSection 1화면 (admin scope DROP). figma node 2-34 매칭 1/1 완료. 상태 4종 (loading/loaded/empty/error), R9 fix 3단계 wireframe 연계 | `~ docs/prd/wireframes/quiz.md` | route: `/` (HomeScreen QuizSection), figma: node 2-34 (375x251px, QuizCard 343x186px), component: QuizSection.jsx, endpoint: GET `/api/quiz/latest`, table: `fun_quiz` | (대기) | agent: prd-wireframe-generator | v1 stale (admin 3화면) 완전 덮어쓰기. figma 매칭 1/1 (미매칭 0). admin n/a (PC 어드민 scope 외). design-sync 권장 — partial-mock, R9 fix 완료 후 진행 |
| 2026-05-09 | quiz | DESIGN-SYNC | 갭 10건 분석: 디자인 토큰 4 / 레이아웃 1(OK) / 구조 차이 1 / 콘텐츠 차이 1 / 통일성 OK 2 / state 미반영 1. figma 갱신 요청 6건 / 코드 수정 제안 0건 (R9 fix 재확인만) | `+ docs/prd/design-sync/quiz.md` | component: QuizSection.jsx, figma: node 2:34, route: `/` (HomeScreen QuizSection) | (대기) | agent: prd-design-sync | 통일성 위반: figma 섹션제목 13px(표준 15px) / border-strong(표준 border) / text-secondary(표준 text-muted). empty state figma hidden=true 미반영. spot-check: 안내문 텍스트 운영 정책 불일치 / 카드 비율 의도 불명 / figma 스크린샷에 v2 scope 외 보상 모달 존재 |
| 2026-05-09 | quiz | OWNER-DECISION | quiz 도메인 한정 ★ 결정 2건 해소 — (a) visible toggle BE 추가 vs FE 토글 제거 → ✅ DROP / (b) V2 `fun_quiz.title` 컬럼 손실 처리 → ✅ DROP. 둘 다 본 라운드 모바일 scope 외 (admin 영역). Phase 3 admin 정리 라운드까지 본 PRD 에서 제외 | `~ docs/prd/domains/quiz.md` (Part B.7) | scope: 모바일 quiz 도메인 한정. _overview.md § 8 글로벌 5건과 별개 | (대기) | manual | v1 IA-CONFIRM (보류) → v2 IA-CONFIRM (DROP) 전환. 글로벌 5건 추적 표는 갱신 X (도메인 한정 결정이라 별개) |
| 2026-05-09 | quiz | IA-CONFIRM | Part B v2 재정립 — 사용자 4 라운드 대화 확정. 모바일 scope = HomeScreen QuizSection 1개 화면으로 단일화. P0×2 (R9 fix dispatch + props / 빈 상태 UX 보존). admin / 신규 기능 / Owner 결정 보류 항목 (visible toggle / V2 title 컬럼) 모두 DROP. figma node-id=2-34 확정 | `~ docs/prd/domains/quiz.md` | route: `/` (HomeScreen QuizSection), endpoint: GET `/api/quiz/latest`, table: `fun_quiz`, cross-domain: home (HomeScreen dispatch 추가), figma: `node-id=2-34` | (대기) | agent: prd-ia-interactive | v2 재정립 — 4 라운드 사용자 대화 (질문 1 → 모바일 scope = HomeScreen QuizSection 만 / 질문 2 → 자동화 4종 결정 / 질문 3 → 우선순위·KPI / figma node-id=2-34 확정). 이전 라운드 보류 (a)/(b) 모두 DROP 채택으로 해소. v1 의 P1×2/P2×2 (admin path 정렬, visible toggle 정책, visible toggle BE 신설, V2 title 컬럼 처리) 모두 DROP — Phase 3 admin 정리 라운드에서 별도 PRD 라운드 |
| 2026-05-09 | quiz | WIREFRAME | 화면기획서 생성 — 화면 3개 (QuizSection 모바일, AdminQuizListPage, AdminQuizFormModal), figma node 미매칭 (B.5 미정), 텍스트 wireframe 모드 | `+ docs/prd/wireframes/quiz.md` | route: `/` (QuizSection), route: `/admin/content/quiz`, component: QuizSection/AdminQuizTable/QuizModal, R9 fix 3단계 + admin path fix 4건 wireframe 연계 | (대기) | agent: prd-wireframe-generator | figma node 미지정 — design-sync 전 figma frame 확보 필요. visible toggle (기능 4) / V2 title 컬럼 (신규 2) 보류 표시. 모바일 R9 fix (Phase 0) 완료 후 design-sync 진행 권장. ⚠ v2 IA-CONFIRM 이후 admin scope DROP 됨 — wireframe 의 admin 화면 2종은 stale, 후속 라운드에서 정리 필요 |
| 2026-05-09 | quiz | IA-CONFIRM | Part B v1 확정 (Auto mode) — P0 2건 (R9 fix: HomeScreen quiz dispatch + path 정정 + props 전달, 빈 상태 UX 보존) / P1 2건 (admin path 정렬, visible toggle 정책 보류) / P2 2건 (visible toggle BE 신설, V2 title 컬럼 처리). 모바일 quiz 단독 화면 신설 안 함. ⚠ 본 row 는 같은 날 v2 재정립으로 대체됨 (위 row 참조) | `~ docs/prd/domains/quiz.md` | route: `/` (HomeScreen QuizSection), endpoint: GET `/api/quiz/latest`, table: `fun_quiz`, cross-domain: home (HomeScreen dispatch 추가) | (대기) | agent: prd-ia-interactive | Auto mode 합리적 가정 기반 합성. Owner 결정 (visible toggle BE 추가 vs FE 토글 제거 / V2 title 컬럼 마이그 vs 표시 변경) 보류 — 후속 라운드. `_overview.md § 7 Phase 0` R9 fix 와 정합. v2 라운드에서 P1/P2 모두 DROP, Owner 결정 보류 → DROP 으로 해소 |
| 2026-05-09 | mobile | DEPRECATE | `domains/mobile/` 폴더 + `MobileHomePage` 통째 폐기 + PRD 파일 제거 (Owner 결정 #3 → ✅ 즉시 폐기 채택) | `- web/src/domains/mobile/**` (17 파일: MobileHomePage.jsx + 8 컴포넌트 jsx/scss), `- docs/prd/domains/mobile.md` | route: 영향 없음 (HomeScreen 이 활성 진입), component: MobileHomePage 등 dead chain 17건, import 0건 (`fe/dead-suspects.md A`, `dead-confirmed.md 1-A`) | (대기) | manual | "도메인 아닌 dead 더미 폴더라 삭제, 기능이 아님" — Owner 명시. Part B 작성 없이 도메인 자체 제거 |
| 2026-05-09 | _meta | INIT | PRD 작업 히스토리 추적 문서 신설 + 3개 PRD agent 에 history append 의무 추가 | `+ docs/prd/_history.md`, `~ .claude/agents/prd-ia-interactive.md`, `~ .claude/agents/prd-wireframe-generator.md`, `~ .claude/agents/prd-design-sync.md` | none (메타 문서) | (대기) | manual | 본 표는 PRD 도메인 작업 시작 전 baseline. 향후 모든 도메인 작업 누적 |

---

## Type 별 최근 작업 빠른 인덱스

각 Type 마지막 N 건 빠른 참조용. 누적 표가 길어지면 빠른 스캔 위해 갱신.

### 최근 IA-CONFIRM
- 2026-05-09 — admin Part B v1 (background 모드, Part A + 횡단 도메인 특성 + Owner clarification 흡수) — 횡단 PC 어드민. 도메인별 admin 흡수 매핑 명시 (coupons/events/notices/quiz/authentication 흡수 / playerCard·community 별도 라운드). 본 도메인은 잔여 횡단 인프라 만: P0 2건 (T1 AdminRoutes 통합 가드 검증 / T3 Step 3a `store/` 6 파일 즉시 삭제) / P1 2건 (T2 Swagger 운영 노출 정책 / T3 Step 3b `domains/admin/` 통째 폐기 후속 라운드). R4/R5/R6 모두 다른 도메인 PRD 흡수 (events/authentication). wireframe / design-sync n/a (PC 어드민)
- 2026-05-09 — authentication Part B v1 (background 모드, Part A 위험 R3/R5/R6 default 진행) — 인프라성. P0 2건 (T1 R3 FE state.auth shape fix Phase 0 차단 / T2 R5 BE `/api/dev/test-token` `@Profile("!prod")`) / P1 1건 (T3 R6 `@EnableMethodSecurity` 활성화). R4 events 흡수로 제외. wireframe / design-sync n/a (인프라성). 글로벌 5건 추적 무관
- 2026-05-09 — historyMode Part B v1 (background 모드, memory 정책 기반 default) — mock-only 분류. P1 1건 (T1 dead `data/HistoryMode.js` 제거). P0/신규 기능 0건. memory 3건 합의 (4 상태 단일 화면 / 글로벌 TopBar / 컴포넌트 분해 최소화). design-sync 보류 (BE 도입 후)
- 2026-05-09 — notices Part B v1 (background 모드, 사용자 default 정책) — P0 5건 (T1 admin 폴더 표준화 / T2 admin update PATCH→PUT `{id}` / T3 admin visible `/{id}/visible` / T4 public detail 단건 fetch / T6 admin 4-capability QA). T5 결번 (횡단 dead 별도 라운드). ★ A.8 Owner 결정 해소 (FE 를 BE 표준에 맞춤). public/admin figma 둘 다 미제공 → wireframe 텍스트 모드
- 2026-05-09 — events Part B v1 정정 라운드 (P0 4건 T1/T2/T3/T5: admin 라우트+AdminEventListPage 신규 / admin 4 capability QA / R4 보안 가드 / dead navigate 제거. T4 결번. 신규 컴포넌트 1건. agent 1차 Edit 에서 T2 hijacking + P2 백로그 임의 추가 → 합의안 그대로 정정)
- 2026-05-09 — coupons Part B v1 (P0 5건 T1~T5: BE 단방향+admin 활성화+정합 검증+시간대 fix+외부 URL HTTPS / P1 T6 마이그 / P2 T7 V1 DROP. figma node 16-624 + 10-2. agent 1차 Edit 정정 라운드 = 합의안 그대로 반영)
- 2026-05-09 — quiz Part B **v2 재정립** (P0×2 / 사용자 4 라운드 대화. v1 P1/P2 DROP, figma node-id=2-34 확정)
- 2026-05-09 — quiz Part B v1 (Auto mode, P0×2 / P1×2 / P2×2) — v2 로 대체됨

### 최근 WIREFRAME
- 2026-05-09 — historyMode wireframe figma node 매핑 보강 (사용자 제공 3 frame 추가 — 66-688/18-2516/66-652. § 7 figma 미반영 사항 3건 추가 + § 7.1 사용자 제공 3 frame 역할 매핑 + § 7.2 figma-updates 명세 cross-ref)
- 2026-05-09 — historyMode 화면기획서 신규 작성 (figma node 4건 매칭 4/4 — 18-1908/18-1949/66-544/66-688, 단일 화면 4상태 wireframe, design-sync 보류 mock-only)
- 2026-05-09 — events 화면기획서 신규 작성 (figma node 16-775 + 3-18 매칭 2/2, admin 텍스트 wireframe, T5 dead navigate 제거 인터랙션, design-sync EventScreen/EventListHorizontal 권장)
- 2026-05-09 — coupons 화면기획서 신규 작성 (figma node 16-624 + 10-2 매칭 2/2, admin 텍스트 wireframe, design-sync Screen 1/2 권장)
- 2026-05-09 — quiz HomeScreen QuizSection v2 완전 재생성 (figma node 2-34 매칭 1/1, admin scope n/a, R9 fix 연계)

### 최근 DESIGN-SYNC
- 2026-05-09 — events public 모바일 (figma 16-775 + 3-18) — 갭 25건, figma 갱신 요청 13건 (P0 2건: 도메인 PageHeader 제거 F1·expired variant inconsistency F2), 코드 수정 제안 1건 (날짜 색상 muted), spot-check 6건 (T5 dead navigate S1 포함)
- 2026-05-09 — coupons public 모바일 (figma 16:624 + 10:2) — 갭 20건, figma 갱신 요청 10건 (P0 2건: 도메인 헤더 제거·PrimaryActionButton 제거), 코드 수정 제안 2건, spot-check 5건
- 2026-05-09 — quiz HomeScreen QuizSection (figma 2:34) — 갭 10건, figma 갱신 요청 6건, spot-check 4건

### 최근 FIGMA-UPDATE-SPEC
- 2026-05-09 — historyMode figma 갱신 명세서 신설 (사용자 제공 3 frame: 66-688/18-2516/66-652 MCP 직접 검증 + 이전 라운드 4 frame 통합). 5 actionable (P0×1 도메인 PageHeader 제거 / P1×3 PlayerRow apostrophe·v0.2.0 제거·StageCard selected variant / P2×1 EmptyState 컴포넌트 단일화). 코드 = source-of-truth (BE 통신 없이 FE 단순 계산 로직 정책 재확인)
- 2026-05-09 — quiz figma 갱신 명세서 신설 (figma 2-34 design-sync 결과 기반). 8 actionable (P0×2 / P1×5 / P2×1)

### 최근 MIGRATE
- 2026-05-09 — historyMode mock 2 파일 위치·이름 재정의: `web/src/data/historyMode/{LegendMeta,LegendStuff}.js` → `web/src/domains/historyMode/config/{MOCK_HISTORY_LEGENDS,MOCK_HISTORY_STAGES}.js` (export identifier `legendMeta`→`MOCK_HISTORY_LEGENDS`, `legendStuff`→`MOCK_HISTORY_STAGES`). 사용처 1건 (`useHistoryMode.js`) import + identifier 갱신. 도메인 표준 mock 패턴 정렬 (`home/config/MOCK_*.js`)

### 최근 CODE
- 2026-05-09 — admin T3 Step 3a — `web/src/domains/admin/store/**` 6 파일 통째 삭제 (api/endpoints/thunks/dto/slices/index. import 0건 grep 검증 + Redux store 미등록). admin.md TODO 섹션 신설. Step 3b (pages/config/feature) 4 consumer 흡수 후 별도 라운드 (TODO P1)
- 2026-05-09 — historyMode T1 dead 파일 제거 — `web/src/data/HistoryMode.js` 삭제 (Part B v1 P1 T1 acceptance 충족)

### 최근 DEPRECATE
- 2026-05-09 — kbo BE 도메인 폐기 — `src/main/java/com/dawne/com2usbaseball/domain/kbo/**` 9 Java 파일 통째 삭제 + `src/main/resources/mapper/kbo/KboGameMapper.xml` 삭제 (mapper 디렉터리 제거). `/api/kbo/matches/today`·`/api/kbo/matches/{matchId}` 2 endpoint 폐기. SecurityConfig 전용 가드 없음 / CacheConfig 이미 전체 주석 — 추가 정리 불필요. ⚠ DB 테이블 (kbo_*) + kbocrol/ + sql/CREATE_TABLE_KBO.sql 등 schema 미터치 (kbocrol Python crawler DB 공유 운영 중). 글로벌 ★ Owner 결정 #4 일부 해소 (kbo 한정)
- 2026-05-09 — kbo FE 도메인 폐기 — `web/src/domains/kbo/**` 17 파일 통째 삭제 + 연관 사용처 4 파일 주석 처리 (PublicRoutes / store.js / QUICK_MENUS / MENU_GROUPS — 추후 복원 가능성 보존). Redux `kbo` slice 미등록. commit `2054c37`. BE 폐기는 별도 commit 분리
- 2026-05-09 — admin `web/src/domains/admin/` 통째 폐기 (T3 Step 3b 완료, 11 파일 삭제) + 4 consumer 흡수 (AdminRoutes 3 라우트 주석 / AdminPageLayout 의 AdminNavigation inline 흡수 / coupons CouponTableBody import 갱신 / VisibleToggle 글로벌 공용 `web/src/global/ui/visibleToggle/` 이전). 직전 라운드 commit 79c8127 (Step 3a store/) 후속 처리 — admin.md TODO P1 → P0 즉시 처리
- 2026-05-09 — admin `web/src/admin/` (legacy PC 버전 admin 샘플) 폐기 + admin.md frontend admin 구현 정책 명시. 작업 시점 폴더 부재 + 외부 import 0건 grep 검증 — 코드 변경 0건 / PRD 문서 기록만. 사용자 정책: frontend admin 은 `src/domains/{domain}/feature/admin/` 패턴 (coupons 적용 사례). 본 task 는 `web/src/domains/admin/` (별도 폴더) 와 무관
- 2026-05-09 — `_cross` `web/src/core/` 폴더 통째 폐기 + 연관 community PC-legacy chain 3 파일 삭제 (`usePostUserFilter` → `CommunityPage.jsx` → `UserCommunityPage.jsx`) + `PublicRoutes.jsx` dead lazy import / 주석 route 줄 제거. 사용자 정책: admin 은 `src/domains/{domain}/feature/admin/` 패턴 (coupons 적용)
- 2026-05-09 — mobile 도메인 통째 폐기 (`web/src/domains/mobile/**` + `docs/prd/domains/mobile.md`). Owner 결정 #3 ✅ (A) 즉시 폐기 채택

### 최근 OWNER-DECISION
- 2026-05-09 — 글로벌 ★ #4 (legacy PC 도메인 운명) ◐ **부분 해소** — kbo 한정 ✅ 폐기 (FE commit `2054c37` + BE commit `<be-commit>`). DB 테이블 (kbo_*) / kbocrol Python crawler / sql/CREATE_TABLE_KBO.sql 미터치 (kbocrol DB 공유 운영). dictionary / simulate 는 별도 라운드 미결
- 2026-05-09 — admin 도메인 한정 결정 — `web/src/domains/admin/` 폴더 운명: ✅ **(B) 통째 폐기 — 단계 분리** (Step 3a P0 즉시 / Step 3b P1 후속 라운드). 4 consumer 흡수 매핑 명시. 글로벌 5건과 별개
- 2026-05-09 — notices 도메인 한정 결정 해소 — `A.8` admin notice update method/path → ✅ **FE 를 BE 표준에 맞춤** (PUT `/admin/notices/{id}` + PATCH `/admin/notices/{id}/visible`). 글로벌 5건과 별개
- 2026-05-09 — 글로벌 ★ #1 coupon dual-write ◐ **부분 해소** — (B) 단방향 정리 + 3 단계 분리 (Step 1 P0 / Step 2 P1 / Step 3 P2 별도 라운드)
- 2026-05-09 — quiz 도메인 한정 결정 2건 해소 — (a) visible toggle ✅ DROP / (b) V2 title 컬럼 ✅ DROP. 모바일 scope 외 → Phase 3 admin 정리 라운드까지 제외. 글로벌 5건과 별개
- 2026-05-09 — 결정 #3 ✅ (A) 즉시 폐기. `domains/mobile/` 통째 제거. 잔여 4건 미결

---

## ★ Owner 결정 5건 추적

`docs/prd/_overview.md § 8` 의 5건 결정 상태. 결정 시 `OWNER-DECISION` row 추가 + 본 섹션도 갱신.

| # | 결정 항목 | 차단성 | 분산 도메인 | 상태 | 결정일 | 결정 내용 |
|---:|---|---|---|---|---|---|
| 1 | coupon dual-write 정책 | 🔥 admin coupon 차단 | coupons | ◐ 부분 해소 | 2026-05-09 | (B) 단방향 정리. 단 V1 `coupons` 테이블 DROP 은 단계 분리 — Step 1 (P0): 코드 단방향 전환, Step 2 (P1): V1→V2 INSERT only 마이그, Step 3 (P2 별도 라운드): V1 DROP |
| 2 | contact ↔ discipline 컬럼 의미 | 🔥 모바일 player_card 차단 | playerCard | ☐ 미결 | — | Owner 도메인 의도 명확화 필요 |
| 3 | MobileHomePage / domains/mobile/ 폐기 | ◐ 직접 차단 X | mobile (주), home | ✅ 결정 | 2026-05-09 | (A) 즉시 폐기. `domains/mobile/` 폴더 + `mobile.md` PRD 통째 제거 완료 |
| 4 | legacy PC 도메인 운명 | ◐ 정리 라운드 | dictionary, simulate, kbo | ◐ 부분 해소 | 2026-05-09 | kbo 한정 ✅ 폐기 (FE commit `2054c37` + BE commit `<be-commit>`). dictionary / simulate 는 별도 라운드 미결 |
| 5 | V2 통폐합 진입 시점 | ⚠ figma 결과 따라 | playerCard (주), coupons | ☐ 미결 | — | figma 도착 시 결정 |

> 결정 시 row 의 상태를 ✅ 로 변경 + 결정일 / 결정 내용 채우고, 누적 표에도 `OWNER-DECISION` 별도 row 추가.

---

## 도메인별 진척도 스냅샷

각 도메인의 PRD pipeline 진행 상태. agent 가 작업 완료 시 갱신 의무.

| Domain | Part A | Part B (IA) | Wireframe | Design-Sync | 코드 반영 | 폐기 |
|---|---|---|---|---|---|---|
| home | ✅ baseline | ☐ | ☐ | ☐ | — | — |
| community | ✅ baseline | ☐ | ☐ | ☐ | — | — |
| coupons (★ 표준) | ✅ baseline | ✅ | ✅ | ✅ | — | — |
| events (★ 표준) | ✅ baseline | ✅ | ✅ | ✅ | — | — |
| notices | ✅ baseline | ✅ | ☐ | ☐ | — | — |
| historyMode | ✅ baseline | ✅ | ✅ | n/a (mock-only) — figma-updates 명세 신설 ✅ 2026-05-09 | ✅ 2026-05-09 (T1 dead 제거) | — |
| profile | ✅ baseline | ☐ | ☐ | ☐ | — | — |
| authentication | ✅ baseline | ✅ | n/a (인프라성) | n/a | — | — |
| quiz | ✅ baseline | ✅ v2 | ✅ v2 | ✅ | — | — |
| playerCard | ✅ baseline | ☐ (figma 도착 시) | ☐ | ☐ | — | — |
| admin | ✅ baseline | ✅ | n/a (PC 어드민) | n/a | — | — |
| dictionary | ✅ baseline | 보류 (legacy PC) | n/a | n/a | — | — |
| simulate | ✅ baseline | 보류 (legacy PC) | n/a | n/a | — | — |
| kbo | ✅ baseline | n/a (폐기 완료) | n/a | n/a | ✅ 2026-05-09 (FE+BE 폐기) | ✅ 2026-05-09 |
| mobile | ✅ baseline (제거됨) | n/a (폐기 완료) | n/a | n/a | ✅ 2026-05-09 | ✅ 2026-05-09 |

### 진척도 마커

- ✅ 완료
- ☐ 미진행 / 대기
- 🔄 진행 중
- n/a 해당 없음 (도메인 분류상)
- 보류 (Owner 정책)

> 도메인 작업 완료 시 해당 셀을 ✅ 로 갱신 + 누적 표에 row 추가. 폐기 시 도메인 row 자체는 본 표에서 삭제 X (히스토리 보존 — 마지막 컬럼 폐기 ✅ 표시).
