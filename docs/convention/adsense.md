# AdSense 광고 슬롯 컨벤션

> 2026-09-13 작성. 광고 영역을 추가·이동·수정하는 모든 작업은 이 문서를 따른다.
> 배경: 2026-08-30 AdSense 반려 사유가 "게시자 콘텐츠가 없는 화면에 광고"였다.
> 재발 방지가 이 컨벤션의 존재 이유다 — **광고는 콘텐츠가 확정적으로 존재하는 자리에만.**

---

## 1. 구조 (수동 슬롯 체계)

자동광고는 쓰지 않는다. 모든 광고는 수동 슬롯이다.

| 파일 | 역할 |
|---|---|
| `web/src/infra/ads/AdSlot.jsx` | `<ins class="adsbygoogle">` 래퍼. 스크립트 미로드 시(승인 전) 조용히 skip, DEV 에선 placeholder box 렌더 |
| `web/src/infra/ads/adConfig.js` | `ADS_ENABLED` 스위치 + `AD_CLIENT_ID` + `AD_SLOTS` 상수. 슬롯 ID 는 승인 전까지 `TODO_*_SLOT_ID` 자리표시자 |
| `web/index.html:39-45` | adsbygoogle 로더 — **승인 전까지 주석 유지.** 소유권 meta(`google-adsense-account`)만 활성 |

**`ADS_ENABLED` (승인 전/후 단일 스위치)**: in-feed 를 위해 표·그리드를 세그먼트로 쪼개는
화면(§ 3 의 "in-feed" 방식들)은 `ADS_ENABLED`가 `false`일 때 **분할 자체를 하지 않는다** —
광고 자리 없이 단일 표/단일 그리드로 통짜 렌더한다. `AdSlot` 자체는 승인 전에도 이미 조용히
skip 하지만, 그것과 별개로 "분할된 여러 컴포넌트 인스턴스"가 만들어지는 순간 세그먼트마다
헤더 반복 여부·컬럼 폭 계산이 달라져 광고가 안 보여도 UI 가 어긋나는 문제가 있었다(2026-09-13
버그). 그래서 분할 여부 자체를 `ADS_ENABLED` 로 게이트한다. 섹션 사이·목록 하단·본문 끝처럼
**단일 삽입**(분할 없이 그 자리에 `<AdSlot>` 하나만 추가하는 방식)은 이미 `AdSlot`이 승인 전
null 렌더라 게이트가 필요 없다 — 홈/쿠폰/이벤트/공지/가이드상세가 여기 해당.

사용 패턴:

```jsx
import AdSlot from "@/infra/ads/AdSlot.jsx";
import { AD_SLOTS } from "@/infra/ads/adConfig.js";

{!loading && !error && list.length > 0 && (
  <AdSlot slot={AD_SLOTS.NOTICES_LIST} />
)}
```

---

## 2. 배치 룰 (모바일 웹 기준 — 위반 금지)

| # | 룰 | 이유 |
|---|---|---|
| 1 | **첫 화면(스크롤 전)에 광고 금지** — 화면 최상단, 가이드 아코디언 근처 X | "콘텐츠보다 광고가 먼저" 정책 위반 |
| 2 | **빈 상태·로딩·에러에서 슬롯 자체를 렌더하지 않는다** — `!loading && !error && 데이터 ≥ 1` 조건 필수 | 반려 사유 1 재발 방지 |
| 3 | **긴 리스트는 하단이 아니라 in-feed** — 첫 삽입은 10번째 아이템 뒤, 이후 30개 간격, 화면당 최대 2개 | 하단은 노출이 안 됨 |
| 4 | **한 뷰포트에 광고 1개** — in-feed 간격을 좁히지 않는다 | 광고 밀도 정책 |
| 5 | **접힌 영역(아코디언·모달·탭 비활성) 내부 금지** | 숨겨진 광고 정책 위반 |
| 6 | 짧은 목록(≈20개 이하)은 목록 하단 1개 허용 | 스크롤 끝이 실제로 보임 |
| 7 | 텍스트 페이지(가이드 상세 등)는 본문 끝 1개 | 콘텐츠 소비 완료 지점 |

**배치 금지 화면**: 로그인/OAuth 콜백(`/auth/callback`) · 404 · `/mypage` · `/admin/*` · `/community` · Suspense fallback · 게이트/안내 전용 화면. `AdSlot.jsx` 상단 주석에도 명시돼 있다.

---

## 3. 현재 배치 인벤토리 (2026-09-13)

| 화면 | 슬롯 상수 | 위치 | 방식 |
|---|---|---|---|
| 홈 | `HOME` | 쿠폰 ↔ 공지 섹션 사이 | 섹션 사이 |
| 쿠폰 | `COUPONS_LIST` | 목록 하단 | 짧은 목록 |
| 이벤트 | `EVENTS_LIST` | 목록 하단 | 짧은 목록 |
| 공지 | `NOTICES_LIST` | 목록 하단 | 짧은 목록 |
| 스킬 백과 | `SKILLS_LIST` | 10번째 아이템 뒤 | in-feed |
| 선수 백과 (카드형) | `PLAYERS_LIST` | 10장·40장 지점 세그먼트 사이, 최대 2개 | in-feed |
| 선수 백과 (표형) | `PLAYERS_TABLE` | 20행 뒤부터 20행 간격 세그먼트 사이, 최대 3개 | in-feed (표 세그먼트 분할) |
| 레전드 재료 | `LEGEND_STATS_LIST` | 표 10행·40행 뒤 `colSpan` 행, 최대 2개 | in-feed (표) |
| 가이드 상세 | `GUIDE_DETAIL` | 본문 끝, "다른 가이드" 링크 위 | 본문 끝 |

표(table) 안에 넣을 때는 레전드 재료의 `colSpan` 행 패턴을 재사용한다 (기존 상세 펼침 행과 동일 구조).
단, 좌우 두 `<table>` 을 나란히 붙인 이중 패널 표(선수 백과 표형 `StatsTable`)는 한 표 안에 colSpan
행을 끼우면 좌우 행이 어긋나므로 예외로 컴포넌트 자체를 세그먼트로 쪼개 사이에 전체 폭 광고를
끼운다(두 번째 세그먼트부터 `hideHead` 로 열 머리글 반복 렌더 생략).

---

## 4. 새 슬롯 추가 절차 (체크리스트)

1. [ ] `adConfig.js` 의 `AD_SLOTS` 에 상수 추가 — 값은 `TODO_{이름}_SLOT_ID`
2. [ ] 화면에 `<AdSlot slot={...}/>` 삽입 — § 2 룰 준수, 렌더 조건 필수
3. [ ] 배치 유형 판단: 짧은 목록=하단 / 긴 리스트=in-feed(10번째 뒤) / 텍스트=본문 끝 / 섹션형=섹션 사이
4. [ ] `npm run build:fast` 통과 확인
5. [ ] dev 서버에서 placeholder 육안 확인 (빈 데이터 상태에서 **안 보이는 것**까지 확인)
6. [ ] 이 문서 § 3 인벤토리에 행 추가

슬롯 이동·제거 시에도 § 3 을 갱신한다. **인벤토리와 코드가 어긋나면 이 문서가 거짓말이 된다.**

---

## 5. 승인 전 / 승인 후 상태 머신

| 단계 | `ADS_ENABLED` | index.html 스크립트 | adConfig 슬롯 ID | 실광고 | in-feed 분할 |
|---|---|---|---|---|---|
| **현재 (심사 대기)** | `false` | 주석 | `TODO_*` | 없음 (DEV placeholder 만) | 없음(단일 표/그리드) |
| 승인 통보 후 | `true` | **주석 해제** | AdSense 콘솔에서 발급받은 실제 ID 로 교체 | 게재 시작 | 세그먼트 분할 재개 |

승인 후 작업 순서:
1. `web/src/infra/ads/adConfig.js` 의 `ADS_ENABLED`를 `true`로
2. AdSense 콘솔 → 광고 → 광고 단위 → 각 배치별 "디스플레이 광고" 단위 생성 (반응형)
3. 발급된 슬롯 ID 를 `adConfig.js` 의 `AD_SLOTS`에 교체
4. `web/index.html` 39-45행 주석 해제
5. 배포 → 실기기에서 게재·레이아웃 확인(특히 in-feed 분할 화면 — 선수 백과 표형/카드형,
   레전드 재료, 스킬 백과 — 세그먼트 경계에서 컬럼·그리드 정렬이 어긋나지 않는지)

⚠️ **승인 전에 스크립트를 켜지 않는다.** 심사 시점 사이트는 "광고 없는 완성 상태"가 안전하다.
⚠️ AdSense 콘솔의 자동광고 설정은 **끔** 유지 — 자동광고가 켜지면 § 2 룰이 무력화된다.

---

## 6. 프리렌더와의 관계

- `AdSlot` 은 프리렌더 스냅샷(`web/scripts/prerender.mjs`)에 빈 `<ins>` 또는 아무것도 남기지 않는다 — 크롤러가 보는 콘텐츠 양에 영향 없음. 광고를 이유로 프리렌더 설정을 바꾸지 말 것
- 가이드 아코디언(`global/ui/guideAccordion`)은 `<details>` 로 항상 DOM 에 렌더된다 — 여기에 광고를 넣으면 룰 5 위반이자 스냅샷 오염. 금지

---

## 7. 관련 파일·경위 참조

- 심사 대응 전체 경위·남은 절차: `test-docs/adsense/README.md`
- ads.txt: `web/public/ads.txt` (`pub-8723423525807131`) — 수정할 일 거의 없음
- noindex 목록: `web/src/infra/seo/routeSeo.js` `NOINDEX_PATHS`
- 반려 이력: 2026-08-30 "콘텐츠 없는 화면 광고 + 낮은 가치 콘텐츠" → 2026-09-13 수동 슬롯 체계·가이드 12편·아코디언으로 대응 완료
