<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dawne.com2usbaseball.domain.skill.repository.mapper.SkillScoreConfigMapper">

    <select id="selectActiveConfigsByTarget"
            resultType="com.dawne.com2usbaseball.domain.skill.entity.SkillScoreConfigEntity"
            parameterType="com.dawne.com2usbaseball.common.enums.Target">
        SELECT skill_code,
               target,
               point,
               condition_type,
               condition_value,
               effect_type,
               effect_point
        FROM skill_score_config
        WHERE target    = #{target}
          AND is_active = true
        ORDER BY id
    </select>

</mapper>
