<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dawne.com2usbaseball.domain.player.repository.mapper.PlayerCardMapper">

    <select id="selectPlayersByPosition" resultType="com.dawne.com2usbaseball.domain.player.entity.PlayerCardEntity" parameterType="com.dawne.com2usbaseball.common.enums.Target">
        SELECT
            id,
            card_code     AS cardCode,
            name,
            team_id       AS teamId,
            role,
            grade,
            overall,
            back_number   AS backNumber,
            birth_date    AS birthDate,
            bat_throw     AS batThrow,
            positions,
            traits,
            attributes,
            created_at    AS createdAt
        FROM player_legend
        WHERE role = #{target}
        ORDER BY name, overall DESC
    </select>
</mapper>
