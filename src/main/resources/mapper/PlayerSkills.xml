<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dawne.com2usbaseball.repository.mapper.PlayerSkillsMapper">

    <select id="findByTarget" resultType="com.dawne.com2usbaseball.entity.PlayerSkillsEntity" parameterType="com.dawne.com2usbaseball.enums.Target">
        SELECT id, skill_code, name, description, grade, target, created_at
        FROM player_skills
        WHERE target = #{target}
        ORDER BY grade, id
    </select>

</mapper>
