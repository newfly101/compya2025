<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dawne.com2usbaseball.domain.fun.team.repository.mapper.FunTeamMapper">

    <resultMap id="TeamResultMap"
               type="com.dawne.com2usbaseball.domain.fun.team.entity.TeamEntity">
        <id property="id" column="id"/>
        <result property="teamCode" column="team_code"/>
        <result property="teamName" column="team_name"/>
        <result property="latestTeamId" column="latest_team_id"/>
        <result property="cityName" column="city_name"/>
        <result property="startYear" column="start_year"/>
        <result property="endYear" column="end_year"/>
        <result property="emblemUrl" column="emblem_url"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="columns">
        id,
        team_code,
        team_name,
        latest_team_id,
        city_name,
        start_year,
        end_year,
        emblem_url,
        created_at,
        updated_at
    </sql>

    <select id="findAll" resultMap="TeamResultMap">
        SELECT
        <include refid="columns"/>
        FROM fun_teams
        ORDER BY team_code, start_year
    </select>

    <select id="findByTeamCode" resultMap="TeamResultMap">
        SELECT
        <include refid="columns"/>
        FROM fun_teams
        WHERE team_code = #{teamCode}
        ORDER BY team_code, start_year
    </select>

</mapper>
